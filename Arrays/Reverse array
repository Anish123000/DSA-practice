#include <iostream>
#include <algorithm>
using namespace std;
int main()
{
    int arr[] = {4, 7, 3, 9, 6};
    reverse(arr, arr + 5);
    for (int i = 0; i < 5; i++)
    {
        cout << arr[i] << " " ;
    }
}
