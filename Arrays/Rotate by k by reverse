
#include<iostream>
#include<algorithm>
using namespace std;

int main(){

    int arr[] = {7,8,3,9,0};
    int n = 5;
    int k = 2;

    reverse(arr, arr+k);
    reverse(arr+k, arr+n);
    reverse(arr, arr+n);

    for(int i=0;i<n;i++){
        cout<<arr[i]<<" ";
    }
}
