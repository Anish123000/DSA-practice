#include <iostream>
using namespace std;
int main()
{

    int arr[] = {2, 4, 6, 8, 9};
    int largest = arr[0];
    for (int i = 1; i < 5; i++)
    {
        largest = max(largest, arr[i]);
    }

    cout << "Largest element is: " << largest << endl;
}
